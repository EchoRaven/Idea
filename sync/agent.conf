# agent-sync 配置（shell 语法，改完即时生效，不用重启 app）

# 1 = agent 改完索引后直接推送到 GitHub
# 0 = 只在本地提交，等你在菜单栏点「推送待确认的改动」
AUTO_PUSH=1

# agent 用哪个模型分析文档
MODEL="claude-sonnet-5"

# 单次最多交给 agent 多少个变化文件（防止一次拉进来几十份把上下文撑爆）
MAX_FILES=25

# agent 单次分析的最长秒数，超过就终止本轮（默认 900 = 15 分钟）
AGENT_TIMEOUT=900
